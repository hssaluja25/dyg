import 'package:flutter_test/flutter_test.dart';
import 'package:spotify/services/API/decrypt.dart';

void main() {
  test(
    'Decryption works. Assumption: The input is a base64 encoded string and decryption key is same as the encryption key used for encrypting.',
    () {
      expect();
    },
  );
}
